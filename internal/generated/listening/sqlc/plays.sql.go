// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: plays.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
)

const countPlays = `-- name: CountPlays :one
SELECT COUNT(*) FROM skill.listening_plays
WHERE user_id = $1 AND content_version_id = $2 AND context_id = $3
`

type CountPlaysParams struct {
	UserID           uuid.UUID
	ContentVersionID uuid.UUID
	ContextID        uuid.UUID
}

func (q *Queries) CountPlays(ctx context.Context, arg CountPlaysParams) (int64, error) {
	row := q.db.QueryRow(ctx, countPlays, arg.UserID, arg.ContentVersionID, arg.ContextID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const recordPlay = `-- name: RecordPlay :one
INSERT INTO skill.listening_plays (
    id,
    user_id,
    content_version_id,
    context_type,
    context_id,
    played_at
) VALUES (
    $1, $2, $3, $4, $5, clock_timestamp()
)
RETURNING id, user_id, content_version_id, context_type, context_id, played_at
`

type RecordPlayParams struct {
	ID               uuid.UUID
	UserID           uuid.UUID
	ContentVersionID uuid.UUID
	ContextType      string
	ContextID        uuid.UUID
}

func (q *Queries) RecordPlay(ctx context.Context, arg RecordPlayParams) (SkillListeningPlay, error) {
	row := q.db.QueryRow(ctx, recordPlay,
		arg.ID,
		arg.UserID,
		arg.ContentVersionID,
		arg.ContextType,
		arg.ContextID,
	)
	var i SkillListeningPlay
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.ContentVersionID,
		&i.ContextType,
		&i.ContextID,
		&i.PlayedAt,
	)
	return i, err
}
