---
module: ai
tier: platform
group: platform
status: PLANNED
phase: 3
owner: "@ai-team"
schema: ai
tables: [ai_requests, ai_usage, prompt_versions, ai_cache_entries, ai_budgets]
depends_on: [cache, telemetry, job]
depended_on_by: [writing, speaking, grammar, questionbank, content, reading, media, learning]
spec_version: 1.0.0
last_verified: 2026-08-06
---

# ai — Testing

Global policy: [`/TESTING_GUIDELINE.md`](../../../TESTING_GUIDELINE.md).

## Targets

| Layer | Target |
|---|---|
| `domain/` | 90 % |
| `service/` | 80 % |
| `repository/` | every query exercised by an integration test |
| `transport/http/` | every endpoint has a contract test |

## What to test

<!-- BEGIN GENERATED: test-focus -->
- Fallback chain: killing provider 1 must produce a correct result from provider 2 with the substitution recorded
- Quota and budget enforcement, including the boundary at exactly the cap
- Schema validation and the single repair attempt
- Score clamping and consistency checks reject an out-of-range score
- Prompt injection: red-team fixtures must not change the output score
- Cache correctness: a different input must never return a cached response
- Cost accounting matches the token counts reported by the provider
- Streaming: a disconnect mid-stream does not lose already-generated content
<!-- END GENERATED: test-focus -->

## Edge cases that have bitten similar modules

<!-- BEGIN GENERATED: test-edges -->
- Two identical requests arriving concurrently — single-flight must produce one provider call
- A provider returning valid JSON that violates the schema in a subtle way (extra field, wrong enum)
- An empty submission, a 10,000-word submission, and one that is entirely emoji
- Budget crossing the cap mid-request
<!-- END GENERATED: test-edges -->

## Fixtures and test data

<!-- BEGIN GENERATED: test-fixtures -->
- Builders in `test/fixtures/builders/ai.go`
- Golden files in `internal/platform/ai/testdata/`
<!-- END GENERATED: test-fixtures -->

## Mocks

<!-- BEGIN GENERATED: test-mocks -->
Generated by `moq` into `internal/platform/ai/mocks/`:

- `cache.Service` — the contract of [`cache`](../../platform/cache/AGENT.md)
- `telemetry.Service` — the contract of [`telemetry`](../../platform/telemetry/AGENT.md)
- `job.Service` — the contract of [`job`](../../platform/job/AGENT.md)

Infrastructure (Postgres, Redis, MinIO) is **not** mocked; integration tests use testcontainers.
<!-- END GENERATED: test-mocks -->

## Running

```bash
go test ./internal/platform/ai/...
go test -tags=integration ./internal/platform/ai/...
go test -run TestXxx -race -v ./internal/platform/ai/...
```

## AI-assisted test generation

Use `docs/prompts/dev/testing/generate-unit-test.md`. Give the agent this module's
`AGENT.md` §9 (business rules) as the oracle — **never the implementation**, or the test will
inherit the implementation's bugs.
