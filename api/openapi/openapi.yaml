openapi: 3.1.0
info:
  title: Fluentra API
  version: 0.1.0
  description: >-
    The versioned HTTP contract for Fluentra. Public operations explicitly set
    `security: []`; every other operation must declare its required permission.
  contact:
    name: Fluentra backend team
    url: https://github.com/fluentra/fluentra
servers:
  - url: /api/v1
    description: Version 1 API
tags:
  - name: system
    description: Platform health and diagnostics.
  - name: user
    description: >-
      The caller's own account: profile and preferences. Every operation here
      acts on the actor in the access token, never on a user id in the path.
  - name: rbac
    description: >-
      Roles and permissions. What the caller may do, and the administrative
      operations that change it.
  - name: audit
    description: >-
      The record of what happened. Append-only: there is no operation here that
      edits or removes an audit entry, and the application database role holds
      no grant that would let one exist.
security:
  - BearerAuth: []
paths:
  /ping:
    get:
      tags: [system]
      operationId: systemPing
      summary: Check API dependency connectivity.
      description: Performs lightweight PostgreSQL and Redis checks for the trace proof.
      security: []
      responses:
        '200':
          description: The API and its immediate dependencies responded.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ping'
              example:
                status: ok
  /health:
    get:
      tags: [system]
      operationId: systemHealth
      summary: Check process liveness.
      description: Returns success when the API process is able to serve requests.
      security: []
      responses:
        '200':
          description: The API process is live.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
              example:
                status: ok
  /ready:
    get:
      tags: [system]
      operationId: systemReady
      summary: Check dependency readiness.
      description: Returns success only when every hard dependency is usable.
      security: []
      responses:
        '200':
          description: All hard dependencies are ready.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ready'
              example:
                status: ready
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /version:
    get:
      tags: [system]
      operationId: systemVersion
      summary: Read the deployed API version.
      description: Returns the version associated with the running API build.
      security: []
      responses:
        '200':
          description: The deployed API version.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Version'
              example:
                version: 0.1.0
  /me:
    get:
      tags: [user]
      operationId: userGetMe
      summary: Read the caller's own account.
      description: >-
        Returns the account belonging to the access token. There is deliberately
        no `/users/{id}` counterpart in this version: the actor comes from the
        token, so one user cannot read another by changing a path segment.
      x-permission: self
      responses:
        '200':
          description: The caller's account.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Me'
              example: &meExample
                id: 0199a1c2-3d4e-7f80-9abc-def012345678
                email: learner@example.com
                status: active
                email_verified_at: '2026-08-09T04:21:07Z'
                created_at: '2026-08-01T09:15:00Z'
                updated_at: '2026-08-09T04:21:07Z'
                profile:
                  display_name: Nghi
                  avatar_url: null
                  country: VN
                  timezone: Asia/Ho_Chi_Minh
                  date_of_birth: '1998-03-04'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [user]
      operationId: userUpdateMe
      summary: Update the caller's own profile.
      description: >-
        Partially updates the profile. Omitted fields are left alone; unknown
        and null fields are rejected with 422.
      x-permission: self
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMeRequest'
            example:
              display_name: Nghi Nguyen
              timezone: Asia/Ho_Chi_Minh
      responses:
        '200':
          description: The updated account.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Me'
              example: *meExample
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
  /me/permissions:
    get:
      tags: [rbac]
      operationId: rbacGetMyPermissions
      summary: Read the caller's own effective permissions.
      description: >-
        Resolves the caller's roles to the flat set of named permissions they
        grant. Advisory only: it exists so the interface can hide actions that
        would fail, and every server call re-checks regardless of what the
        client believes.
      x-permission: self
      responses:
        '200':
          description: The caller's roles and permissions.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MyPermissions'
              example:
                roles: [user]
                permissions: []
        '401':
          $ref: '#/components/responses/Unauthorized'
  /me/preferences:
    get:
      tags: [user]
      operationId: userGetMyPreferences
      summary: Read the caller's preferences.
      description: >-
        Returns the full preference set. Every account has one from
        registration onwards, so this never returns an empty body.
      x-permission: self
      responses:
        '200':
          description: The caller's preferences.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Preferences'
              example: &preferencesExample
                locale: vi
                theme: dark
                daily_goal_minutes: 30
                notification_channels: [in_app, push]
                quiet_hours:
                  start: '22:00'
                  end: '07:00'
                ai_processing_opt_out: false
                updated_at: '2026-08-09T04:21:07Z'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [user]
      operationId: userReplaceMyPreferences
      summary: Replace the caller's preferences.
      description: >-
        Replaces the whole preference set. A field left out is not "unchanged",
        it is a validation failure -- which is what makes this idempotent.
      x-permission: self
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReplacePreferencesRequest'
            example:
              locale: vi
              theme: dark
              daily_goal_minutes: 30
              notification_channels: [in_app, push]
              quiet_hours:
                start: '22:00'
                end: '07:00'
              ai_processing_opt_out: false
      responses:
        '200':
          description: The stored preferences.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Preferences'
              example: *preferencesExample
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
  /admin/roles:
    get:
      tags: [rbac]
      operationId: rbacListRoles
      summary: List roles and the permissions they grant.
      description: >-
        The catalogue is small and fixed, so it is returned whole rather than
        paginated.
      x-permission: rbac.read
      responses:
        '200':
          description: Every role, with its permissions.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoleList'
              example:
                items:
                  - name: admin
                    description: Full administrative access.
                    permissions: [rbac.assign, rbac.read, user.list]
                  - name: user
                    description: A learner. Holds no named permissions.
                    permissions: []
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /admin/users/{id}/roles:
    parameters:
      - name: id
        in: path
        required: true
        description: The user whose roles are being changed.
        schema:
          type: string
          format: uuid
    post:
      tags: [rbac]
      operationId: rbacAssignRole
      summary: Grant a role to a user.
      description: >-
        Granting is idempotent: a role the user already holds is not an error.
        An actor cannot grant themselves a role they do not already hold.
      x-permission: rbac.assign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignRoleRequest'
            example:
              role: admin
      responses:
        '200':
          description: The user's roles after the grant.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoles'
              example: &userRolesExample
                user_id: 0199a1c2-3d4e-7f80-9abc-def012345678
                roles: [admin, user]
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationFailed'
  /admin/users/{id}/roles/{role}:
    parameters:
      - name: id
        in: path
        required: true
        description: The user whose roles are being changed.
        schema:
          type: string
          format: uuid
      - name: role
        in: path
        required: true
        description: The role to revoke.
        schema:
          $ref: '#/components/schemas/RoleName'
    delete:
      tags: [rbac]
      operationId: rbacRevokeRole
      summary: Revoke a role from a user.
      description: >-
        Revoking is idempotent. Two revocations are refused: an actor removing
        their own `admin` role, and any revocation that would leave the system
        with no administrator.
      x-permission: rbac.assign
      responses:
        '200':
          description: The user's roles after the revocation.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoles'
              example: *userRolesExample
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /admin/audit-logs:
    get:
      tags: [audit]
      operationId: auditSearchLogs
      summary: Search the audit trail.
      description: >-
        Returns audit entries newest first, within a bounded time window.


        The window is mandatory in effect: `to` defaults to now and `from` to 90
        days before `to`. The table is partitioned by month on `created_at`, and
        a query with no bound on it reads every partition ever created — so the
        default is a bounded scan rather than an unbounded one that happens to
        be fast while the table is young.
      x-permission: audit.read
      parameters:
        - name: actor_id
          in: query
          required: false
          description: Only entries recorded against this actor.
          schema:
            type: string
            format: uuid
        - name: action
          in: query
          required: false
          description: Exact action name, such as `user.profile_updated`.
          schema:
            $ref: '#/components/schemas/AuditAction'
        - name: target_type
          in: query
          required: false
          description: Only entries whose target is of this kind.
          schema:
            type: string
            maxLength: 64
        - name: target_id
          in: query
          required: false
          description: >-
            Only entries against this target. Combined with `target_type` it is
            the "who touched this record" query.
          schema:
            type: string
            maxLength: 128
        - name: from
          in: query
          required: false
          description: Inclusive lower bound on `created_at`. Defaults to 90 days before `to`.
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          required: false
          description: Exclusive upper bound on `created_at`. Defaults to now.
          schema:
            type: string
            format: date-time
        - name: cursor
          in: query
          required: false
          description: Opaque cursor from the previous page's `next_cursor`.
          schema:
            $ref: '#/components/schemas/Cursor'
        - name: limit
          in: query
          required: false
          description: Maximum entries to return.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: One page of audit entries.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLogPage'
              example:
                items:
                  - id: 0199a1c2-3d4e-7f80-9abc-def012345678
                    created_at: '2026-08-10T09:15:00Z'
                    actor_id: 0199a1c2-3d4e-7f80-9abc-def012345679
                    actor_role: user
                    action: user.profile_updated
                    target_type: user
                    target_id: 0199a1c2-3d4e-7f80-9abc-def012345679
                    changed_fields: [display_name, timezone]
                    before: null
                    after: null
                    meta:
                      source: outbox
                    trace_id: 4bf92f3577b34da6a3ce929d0e0e4736
                page:
                  has_more: false
                  limit: 20
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationFailed'
  /admin/security-events:
    get:
      tags: [audit]
      operationId: auditSearchSecurityEvents
      summary: Read the security event feed.
      description: >-
        Returns security events newest first, within the same bounded window as
        the audit trail and for the same reason. `resolved=false` is the triage
        queue.
      x-permission: audit.read
      parameters:
        - name: kind
          in: query
          required: false
          description: Exact event kind, such as `rbac.access_denied`.
          schema:
            type: string
            pattern: '^[a-z][a-z0-9_]*\.[a-z][a-z0-9_]*$'
        - name: severity
          in: query
          required: false
          description: Only events at this severity.
          schema:
            $ref: '#/components/schemas/SecurityEventSeverity'
        - name: resolved
          in: query
          required: false
          description: >-
            `false` returns only open events, `true` only triaged ones. Omitted
            returns both.
          schema:
            type: boolean
        - name: user_id
          in: query
          required: false
          description: Only events involving this account.
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          required: false
          description: Inclusive lower bound on `created_at`. Defaults to 90 days before `to`.
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          required: false
          description: Exclusive upper bound on `created_at`. Defaults to now.
          schema:
            type: string
            format: date-time
        - name: cursor
          in: query
          required: false
          description: Opaque cursor from the previous page's `next_cursor`.
          schema:
            $ref: '#/components/schemas/Cursor'
        - name: limit
          in: query
          required: false
          description: Maximum events to return.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: One page of security events.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecurityEventPage'
              example:
                items:
                  - id: 0199a1c2-3d4e-7f80-9abc-def01234567a
                    created_at: '2026-08-10T09:15:00Z'
                    kind: rbac.access_denied
                    severity: medium
                    user_id: 0199a1c2-3d4e-7f80-9abc-def012345679
                    detail:
                      permission: user.suspend
                    trace_id: 4bf92f3577b34da6a3ce929d0e0e4736
                    resolved_at: null
                    resolved_by: null
                    resolution_note: null
                page:
                  has_more: false
                  limit: 20
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/ValidationFailed'
  /admin/security-events/{id}/resolve:
    parameters:
      - name: id
        in: path
        required: true
        description: The security event being triaged.
        schema:
          type: string
          format: uuid
    post:
      tags: [audit]
      operationId: auditResolveSecurityEvent
      summary: Mark a security event triaged.
      description: >-
        Records who closed the event and why. Resolving an already-resolved
        event is a conflict rather than a silent overwrite: the first
        explanation is the one that describes what was investigated.
      x-permission: audit.manage
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolveSecurityEventRequest'
            example:
              note: Known load test, permission denial expected.
      responses:
        '200':
          description: The resolved event.
          headers:
            X-Request-Id:
              $ref: '#/components/headers/X-Request-Id'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SecurityEvent'
              example:
                id: 0199a1c2-3d4e-7f80-9abc-def01234567a
                created_at: '2026-08-10T09:15:00Z'
                kind: rbac.access_denied
                severity: medium
                user_id: 0199a1c2-3d4e-7f80-9abc-def012345679
                detail:
                  permission: user.suspend
                trace_id: 4bf92f3577b34da6a3ce929d0e0e4736
                resolved_at: '2026-08-10T10:02:00Z'
                resolved_by: 0199a1c2-3d4e-7f80-9abc-def012345678
                resolution_note: Known load test, permission denial expected.
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/ValidationFailed'
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: A short-lived Fluentra access token.
    RefreshCookie:
      type: apiKey
      in: cookie
      name: refresh_token
      description: A rotating refresh token supplied only to the refresh operation.
  schemas:
    Problem:
      $ref: ./components/errors.yaml#/Problem
    ValidationProblem:
      $ref: ./components/errors.yaml#/ValidationProblem
    ValidationError:
      $ref: ./components/errors.yaml#/ValidationError
    Page:
      $ref: ./components/pagination.yaml#/Page
    Cursor:
      $ref: ./components/pagination.yaml#/Cursor
    Ping:
      $ref: ./components/system.yaml#/Ping
    Health:
      $ref: ./components/system.yaml#/Health
    Ready:
      $ref: ./components/system.yaml#/Ready
    Version:
      $ref: ./components/system.yaml#/Version
    Me:
      $ref: ./components/user.yaml#/Me
    MeProfile:
      $ref: ./components/user.yaml#/MeProfile
    UserStatus:
      $ref: ./components/user.yaml#/UserStatus
    UpdateMeRequest:
      $ref: ./components/user.yaml#/UpdateMeRequest
    Preferences:
      $ref: ./components/user.yaml#/Preferences
    QuietHours:
      $ref: ./components/user.yaml#/QuietHours
    ReplacePreferencesRequest:
      $ref: ./components/user.yaml#/ReplacePreferencesRequest
    MyPermissions:
      $ref: ./components/rbac.yaml#/MyPermissions
    RoleName:
      $ref: ./components/rbac.yaml#/RoleName
    Role:
      $ref: ./components/rbac.yaml#/Role
    RoleList:
      $ref: ./components/rbac.yaml#/RoleList
    AssignRoleRequest:
      $ref: ./components/rbac.yaml#/AssignRoleRequest
    UserRoles:
      $ref: ./components/rbac.yaml#/UserRoles
    AuditLog:
      $ref: ./components/audit.yaml#/AuditLog
    AuditAction:
      $ref: ./components/audit.yaml#/AuditAction
    ActorRole:
      $ref: ./components/audit.yaml#/ActorRole
    AuditLogPage:
      $ref: ./components/audit.yaml#/AuditLogPage
    SecurityEvent:
      $ref: ./components/audit.yaml#/SecurityEvent
    SecurityEventSeverity:
      $ref: ./components/audit.yaml#/SecurityEventSeverity
    SecurityEventPage:
      $ref: ./components/audit.yaml#/SecurityEventPage
    ResolveSecurityEventRequest:
      $ref: ./components/audit.yaml#/ResolveSecurityEventRequest
  headers:
    X-Request-Id:
      $ref: ./components/headers.yaml#/X-Request-Id
    ETag:
      $ref: ./components/headers.yaml#/ETag
    Cache-Control:
      $ref: ./components/headers.yaml#/Cache-Control
    RateLimit-Limit:
      $ref: ./components/headers.yaml#/RateLimit-Limit
    RateLimit-Remaining:
      $ref: ./components/headers.yaml#/RateLimit-Remaining
    RateLimit-Reset:
      $ref: ./components/headers.yaml#/RateLimit-Reset
    Retry-After:
      $ref: ./components/headers.yaml#/Retry-After
  responses:
    BadRequest:
      $ref: ./components/responses.yaml#/BadRequest
    Unauthorized:
      $ref: ./components/responses.yaml#/Unauthorized
    Forbidden:
      $ref: ./components/responses.yaml#/Forbidden
    NotFound:
      $ref: ./components/responses.yaml#/NotFound
    Conflict:
      $ref: ./components/responses.yaml#/Conflict
    ValidationFailed:
      $ref: ./components/responses.yaml#/ValidationFailed
    TooManyRequests:
      $ref: ./components/responses.yaml#/TooManyRequests
    InternalServerError:
      $ref: ./components/responses.yaml#/InternalServerError
    ServiceUnavailable:
      $ref: ./components/responses.yaml#/ServiceUnavailable
