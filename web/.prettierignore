# Generated by `pnpm run gen:api` from api/openapi/openapi.yaml.
#
# CI regenerates this file and fails if it differs from what is committed, so it
# has to stay byte-for-byte what openapi-typescript emits. Prettier reformats it
# — which meant `make fmt`, the command the contributor guide tells everybody to
# run before finishing, quietly broke the frontend staleness gate.
src/types/api.ts
