{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "River Queue Depth",
      "type": "timeseries",
      "targets": [
        {
          "expr": "river_queue_depth",
          "legendFormat": "{{queue}}"
        }
      ]
    },
    {
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Job Failure Rate",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(river_job_completed_total{state=\"failed\"}[5m]) / rate(river_job_completed_total[5m])",
          "legendFormat": "Failure %"
        }
      ]
    }
  ],
  "refresh": "10s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["fluentra", "jobs"],
  "time": { "from": "now-1h", "to": "now" },
  "title": "Jobs & Queues Overview",
  "uid": "fluentra-jobs"
}
